import 'package:flutter/material.dart';

class AppConstants {
  static const String appName = 'size change';
  static final ColorScheme colorScheme = ColorScheme.fromSeed(
    seedColor: Colors.deepPurple,
  );
}
